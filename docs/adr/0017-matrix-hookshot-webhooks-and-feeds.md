# matrix-hookshot is the front door for GitHub, generic webhooks, and feeds — and the aionui notifier becomes a webhook shim

> **Update (2026-06-25):** AionUi and its notifier were removed (see [ADR-0018](0018-claude-relay-matrix-interface.md)), so the `aionui-hookshot-provision` oneshot + `#aionui-alerts` connection described below are gone. The hookshot core — GitHub notifications/repo-events, generic webhooks, feeds — is unchanged and remains.

The fleet needed GitHub notifications in Matrix. Rather than build another bespoke poller, the matrix profile now runs [matrix-hookshot](https://matrix-org.github.io/matrix-hookshot/) (`services.matrix-hookshot`, nixpkgs 7.3.3) on kelpy as a fourth appservice alongside whatsapp and the jmap bridge. It bridges four things at once: GitHub personal notifications (the bell-icon feed, per-user OAuth), GitHub repo events (issues/PRs/CI via a GitHub App webhook), generic inbound webhooks (any service that can POST), and RSS/Atom feeds. Connections are created at runtime by talking to the `@hookshot` bot in a room — no per-connection Nix config.

It slots into the existing declarative-appservice pattern unchanged ([ADR-0006](0006-tuwunel-matrix-homeserver.md)): a sops-rendered `hookshot-registration.yaml` is contributed to `custom.profiles.matrix.appservices`, and tuwunel loads it from `appservice_dir` like every other bridge.

## Considered Options

- **A bespoke GitHub-notifications poller** (mirroring the aionui notifier) — rejected: it would only ever do GitHub, reusing the existing `GH_TOKEN`. Hookshot covers GitHub *plus* generic webhooks, feeds, GitLab and JIRA for later, from one service. Once we wanted "more than GitHub," the poller stopped being the cheap option.
- **Hookshot, config via the nixpkgs `settings` option** — rejected for the secret-bearing parts: the module renders `settings` into a **world-readable Nix store** `config.yml`, but hookshot keeps the GitHub webhook secret and OAuth `client_secret` *inline* (only the App private key takes a file path). So we render the whole `config.yml` via a **sops template** and `lib.mkForce`-override `ExecStart` to consume it — the same secret-handling stance the whatsapp/jmap registrations already take. `settings` is still set for `passFile` (the module's preStart generates the token-encryption key there).
- **Hookshot as root (module default)** — rejected: it is internet-facing. It runs as a dedicated static system user `matrix-hookshot` with `StateDirectory` + systemd hardening. Static (not `DynamicUser`) precisely so sops can `chown` the rendered config/registration/key to it — the classic sops-vs-DynamicUser uid clash that [the jmap bridge](0007-custom-jmap-matrix-bridge.md) sidesteps with `LoadCredential`.

## Consequences

- **Two ports, one public.** The appservice port (9993, loopback) is internal and lives in the module; the webhook/OAuth listener (9000) is registered in `parts/settings.nix` as `services.public.hookshot`, so `proxy.nix` auto-fronts `hookshot.palebluebytes.space` and the `dns` app auto-emits its record (grey-clouded, `proxy` omitted). GitHub and generic senders POST there; the OAuth callback is `…/oauth`.
- **The aionui notifier ([ADR-0008](0008-aionui-matrix-via-rest-poller.md)) becomes webhook-only and fully declarative.** aioncore still cannot push, so a poller is still required — but it now only POSTs each event as `{"text": …}` to a hookshot generic webhook (hookshot renders `text`/`html` directly, no JS transform needed). The notifier's entire Matrix side — login, registration token, bot password, room creation — is **deleted**, not kept as a fallback. Net: one fewer Matrix bot, one fewer self-registered account, no notifier secret.
- **The aionui connection is provisioned declaratively, no bot command.** A `aionui-hookshot-provision` oneshot reuses the hookshot appservice `as_token` to create the `#aionui-alerts` room and the generic-webhook connection. Hookshot stores a generic hook's id in the bot's *room account data* (mapping `hookId → stateKey`) and the connection in a `uk.half-shot.matrix-hookshot.generic.hook` state event; the provisioner writes both with a **persisted, self-generated secret hookId**, so the webhook URL (`…/webhook/<hookId>`) is deterministic and is written to the file the notifier reads. (Hookshot also supports config-defined "static" connections, where the state key *is* the hookId — but those need the `roomId` at build time, which a server-generated id can't satisfy; provisioning at runtime via the API sidesteps that.) The dependency on hookshot's internal account-data/state schema is the tradeoff for zero-touch.
- **The GitHub App is the one un-Nix-able bootstrap.** GitHub has no API (nor `gh` command) to create an App, so it is made once by hand in the UI; its id/private-key/webhook-secret/OAuth-client land in `secrets/profiles/matrix.yaml` as `hookshot_github_*`, alongside the random `hookshot_as_token`/`hookshot_hs_token`. The App's webhook URL is `…/github/webhook` (the bare `/` is deprecated) and its OAuth callback is `…/oauth`. For personal notifications alone the App needs **no installation** — the OAuth `github login` user token already spans every account/org you can see; installing it (personal or org) is only for repo-event firehose rooms. **Correction (2026-08-21): the notification feed cannot use the App's token at all.** `GET /notifications` [only supports classic personal access tokens](https://docs.github.com/en/rest/activity/notifications) (`notifications` or `repo` scope); a GitHub App user-to-server token gets `Resource not accessible by integration`, and no App permission changes that — hookshot's `github login` doesn't even request a scope, because for an App it would be meaningless. So `github login` is the right bootstrap for repo/project commands but a dead end for the feed: that needs `github setpersonaltoken <classic PAT>` in the admin room instead. Hookshot's own setup docs don't mention this.
- **passkey.pem must persist.** It encrypts hookshot's stored GitHub/OAuth tokens; losing it forces re-auth of every connection. The whole `/var/lib/matrix-hookshot` state dir is added to impermanence.
- **GitHub/feeds connections are still bot-command driven** (`github login`, `github notifications toggle`, `feed`); GitHub *notifications* can't be declarative anyway (per-user OAuth). Neither the provisioning widget nor metrics listeners are exposed — the public listener serves only the `webhooks` resource.
- **The notification stream position is seeded for hookshot, or the watcher never starts.** `github notifications toggle` hands `UserNotificationWatcher.addUser` a `since` read from the bot's `uk.half-shot.matrix-hookshot.github.notif_state` room account data, which is `0` on a room that has never polled — and `addUser` does `data.since || existing?.since`, so that legitimate `0` reads as absent and it throws `` `since` value missing from data payload `` ([upstream #965](https://github.com/matrix-org/matrix-hookshot/issues/965), open since 2024). Only the watcher itself ever writes `notif_state`, so it can never start: a deadlock that survives every restart, and that a `matrix-reset` reintroduces because the account data goes with the homeserver. The `matrix-hookshot-adminroom` oneshot therefore seeds `notif_state` with "now" (ms) whenever it is unset, alongside the admin-room marker — same masquerade, same idempotence, and "now" is the position we want anyway (new notifications, not the whole backlog). Symptom when it is missing: `github login` succeeds, `notifications toggle` says "Enabled", and *nothing* ever arrives; `journalctl -u matrix-hookshot | grep 'into the notif queue'` is empty. Both markers are the bridge's only load-bearing homeserver-side state, so the oneshot is split into an importable builder (`hookshot-adminroom.nix`, the same shape as `dm-provision.nix`) and pinned by a VM check (`parts/checks/hookshot`) that drives the real script against a real tuwunel: marker written, `since` seeded in **milliseconds** (`GitHubWatcher` does `new Date(since)`, so a seconds-scale value would replay the whole backlog), a live position never rewound, and an already-activated room still healed — the seed sits *before* the restart-once early exit precisely so an existing deployment recovers without a `matrix-reset`.
- **The notification stream position is seeded for hookshot, or the watcher never starts.** `github notifications toggle` hands `UserNotificationWatcher.addUser` a `since` read from the bot's `uk.half-shot.matrix-hookshot.github.notif_state` room account data, which is `0` on a room that has never polled — and `addUser` does `data.since || existing?.since`, so that legitimate `0` reads as absent and it throws `` `since` value missing from data payload `` ([upstream #965](https://github.com/matrix-org/matrix-hookshot/issues/965), open since 2024). Only the watcher itself ever writes `notif_state`, so it can never start: a deadlock that survives every restart, and that a `matrix-reset` reintroduces because the account data goes with the homeserver. The `matrix-hookshot-adminroom` oneshot therefore seeds `notif_state` with "now" (ms) whenever it is unset, alongside the admin-room marker — same masquerade, same idempotence, and "now" is the position we want anyway (new notifications, not the whole backlog). Symptom when it is missing: `github login` succeeds, `notifications toggle` says "Enabled", and *nothing* ever arrives; `journalctl -u matrix-hookshot | grep 'into the notif queue'` is empty. Both markers are the bridge's only load-bearing homeserver-side state, so the oneshot is split into an importable builder (`hookshot-adminroom.nix`, the same shape as `dm-provision.nix`) and pinned by a VM check (`parts/checks/hookshot`) that drives the real script against a real tuwunel: marker written, `since` seeded in **milliseconds** (`GitHubWatcher` does `new Date(since)`, so a seconds-scale value would replay the whole backlog), a live position never rewound, and an already-activated room still healed — the seed sits *before* the restart-once early exit precisely so an existing deployment recovers without a `matrix-reset`.
- **The personal notification feed can live in its own room, declaratively.** Hookshot has no "post notifications to room X" setting — the feed lands in whichever *admin room* it was enabled in (`onAdminRoomSettingsChanged` pushes `roomId: adminRoom.roomId`). But an admin room is only a room whose bot account-data `uk.half-shot.matrix-hookshot.github.room` names an `admin_user`, and on tuwunel that marker is already the sole way a room becomes one. So `custom.profiles.matrix.hookshot.notificationsRoom.enable` provisions a named **GitHub Notifications** room (`hookshot-notifications-room.nix`, created by admin password-login — the appservice masquerade can't `createRoom` on tuwunel, same constraint as #infra-alerts) and points a *second* instance of the admin-room builder at it. That instance also asserts `github.notifications.enabled = true`, which is the only thing `github notifications toggle` writes — so the toggle is declarative too; only `github login` stays manual, being per-user OAuth. The DM instance correspondingly asserts the feed **off**: `UserNotificationWatcher` keys on `userId:type`, so two admin rooms with the feed on means whichever loads last silently wins. Unlike #infra-alerts this needs no two-phase `roomId` pinning — nothing is emitted into hookshot's build-time config, so the server-assigned id just persists in the unit's `StateDirectory`. Writes are read-modify-write (hookshot's own `participating` key survives) and any write forces one bridge restart, because a running hookshot only ever reads this account data at startup.
- **The notification stream position is seeded for hookshot, or the watcher never starts.** `github notifications toggle` hands `UserNotificationWatcher.addUser` a `since` read from the bot's `uk.half-shot.matrix-hookshot.github.notif_state` room account data, which is `0` on a room that has never polled — and `addUser` does `data.since || existing?.since`, so that legitimate `0` reads as absent and it throws `` `since` value missing from data payload `` ([upstream #965](https://github.com/matrix-org/matrix-hookshot/issues/965), open since 2024). Only the watcher itself ever writes `notif_state`, so it can never start: a deadlock that survives every restart, and that a `matrix-reset` reintroduces because the account data goes with the homeserver. The `matrix-hookshot-adminroom` oneshot therefore seeds `notif_state` with "now" (ms) whenever it is unset, alongside the admin-room marker — same masquerade, same idempotence, and "now" is the position we want anyway (new notifications, not the whole backlog). Symptom when it is missing: `github login` succeeds, `notifications toggle` says "Enabled", and *nothing* ever arrives; `journalctl -u matrix-hookshot | grep 'into the notif queue'` is empty. Both markers are the bridge's only load-bearing homeserver-side state, so the oneshot is split into an importable builder (`hookshot-adminroom.nix`, the same shape as `dm-provision.nix`) and pinned by a VM check (`parts/checks/hookshot`) that drives the real script against a real tuwunel: marker written, `since` seeded in **milliseconds** (`GitHubWatcher` does `new Date(since)`, so a seconds-scale value would replay the whole backlog), a live position never rewound, and an already-activated room still healed — the seed sits *before* the restart-once early exit precisely so an existing deployment recovers without a `matrix-reset`.
- **The personal notification feed can live in its own room, declaratively.** Hookshot has no "post notifications to room X" setting — the feed lands in whichever *admin room* it was enabled in (`onAdminRoomSettingsChanged` pushes `roomId: adminRoom.roomId`). But an admin room is only a room whose bot account-data `uk.half-shot.matrix-hookshot.github.room` names an `admin_user`, and on tuwunel that marker is already the sole way a room becomes one. So `custom.profiles.matrix.hookshot.notificationsRoom.enable` provisions a named **GitHub Notifications** room (`hookshot-notifications-room.nix`, created by admin password-login — the appservice masquerade can't `createRoom` on tuwunel, same constraint as #infra-alerts) and points a *second* instance of the admin-room builder at it. That instance also asserts `github.notifications.enabled = true`, which is the only thing `github notifications toggle` writes — so the toggle is declarative too; only `github login` stays manual, being per-user OAuth. The DM instance correspondingly asserts the feed **off**: `UserNotificationWatcher` keys on `userId:type`, so two admin rooms with the feed on means whichever loads last silently wins. Unlike #infra-alerts this needs no two-phase `roomId` pinning — nothing is emitted into hookshot's build-time config, so the server-assigned id just persists in the unit's `StateDirectory`. Writes are read-modify-write (hookshot's own `participating` key survives) and any write forces one bridge restart, because a running hookshot only ever reads this account data at startup.
- **The GitHub token for the feed is stored from sops, not typed into a room.** Since `github login` cannot drive the feed (above), the only supported route is hookshot's `github setpersonaltoken` — which means pasting a credential into the `@hookshot` DM, whose history is unencrypted and lives on the homeserver forever. `custom.profiles.matrix.hookshot.personalToken` does the identical write from a sops secret instead. `storeUserToken` sets the bot's **global** account data `uk.half-shot.matrix-hookshot.github.password-store:<mxid>` to `{ encrypted: [<base64 of each RSA-PKCS#1v15 chunk>], keyId: <md5 hex of passkey.pem>, algorithm: "rsa-pkcs1v15" }` — 128-byte chunks, standard padded base64, per `src/tokens/mod.rs` and `src/format_util.rs` — and the module reproduces exactly that with `openssl`, so hookshot reads it back through its own unmodified code path. That is a dependency on an internal format with no API contract, so `parts/checks/hookshot/token.nix` **decrypts what the unit wrote** with the real private key and asserts the plaintext round-trips, rather than eyeballing the shape; it also covers rotating the token, rotating `passkey.pem` (the `keyId` must follow, or hookshot reports "no token" instead of a key mismatch), and the trailing newline sops emits, which would otherwise be encrypted into the credential. Idempotence keys on the *inputs* (sha256 of token + keyId), because PKCS#1 v1.5 is randomised and the ciphertext differs every run; a write restarts hookshot once, since a running bridge only loads this at startup. The unit also probes `/notifications` with the token at deploy time and warns loudly if it is rejected — the alternative is `GitHubWatcher` logging `Resource not accessible by integration` every 15 seconds with nobody watching.
